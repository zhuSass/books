export default class KuaiYan {
    // 获取首页分类强推
    static getHomeClassifyList($:any) {
        const novellistObj = $('.novellist');
        novellistObj.each((index:number,element:any) => {
            const itemDom = $(element).find('.item');
            
        });
    }
};
